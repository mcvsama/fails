<?php # vim: set fenc=utf8 ts=4 sw=4:

# Environment:
Fails::$config->fails->environment = 'development';

# Replace following with random string, ie. generated by uuid-gen program:
Fails::$config->fails->session_id = 'test';

# Set following, if you don't want Fails to detect base-url automatically:
Fails::$config->fails->base_url = null;

# Whether to display exception dumps of uncaught exceptions (or predefined error pages otherwise):
Fails::$config->fails->render_exceptions = false;

# Whether to display PHP errors (warnings/notices/etc):
Fails::$config->fails->display_errors = true;

# Error reporting threshold:
Fails::$config->fails->display_errors_threshold = E_ALL;

# Auto rendering:
Fails::$config->fails->auto_rendering = false;

# Error pages:
Fails::$config->fails->error_404_file = 'error-404.html';
Fails::$config->fails->error_500_file = 'error-500.html';

# Whether to enable no-cache headers:
Fails::$config->fails->prevent_caching = false;

# Whether to enable automatic ETagging of rendered body:
Fails::$config->fails->auto_etagging = true;

# Output compression:
Fails::$config->fails->output_compression = true;

require 'exception_notifier.inc';

?>
